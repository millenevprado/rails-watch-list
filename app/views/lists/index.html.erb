<h1>My Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= list.name %> | <%= link_to 'see details', list_path(list) %>
    </li>
  <% end %>
</ul>

<hr>
<%= link_to 'create a new list', new_list_path %>
